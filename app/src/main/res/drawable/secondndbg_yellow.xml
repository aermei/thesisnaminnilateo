<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Set the background color for the shape -->
    <solid android:color="#FFF200" /> <!-- You can choose any color here -->

    <!-- Define corner radii: Round only the top left and top right -->
    <corners
        android:topLeftRadius="250dp"
        android:topRightRadius="250dp"
        android:bottomLeftRadius="0dp"
        android:bottomRightRadius="0dp" />

    <!-- Optional padding to make the shape look better -->
    <padding
        android:left="10dp"
        android:right="10dp"
        android:bottom="10dp"
        android:top="10dp" />
</shape>